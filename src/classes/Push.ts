export default interface PushTopic {
  topic: string;
  topicSize: number;
}

export interface PushSetting {
  allSetting: boolean;
  comment: boolean;
  hotBoard: boolean;
  notice: boolean;
  verification: boolean;
}
